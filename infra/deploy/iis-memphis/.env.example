# Copy to .env on the box and set a real password.
PORT=4000
NODE_ENV=production
DATABASE_URL=postgres://vellumrift:CHANGE_ME@hasura-postgres:5432/vellum_rift
# Auth is a dev pass-through until Bluekey SSO is wired (see backend/.env.example).
AUTH_REQUIRED=false
